# 변경 이력

이 프로젝트의 주요 변경 사항을 버전별로 기록합니다.
형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.0.0/)를 참고합니다.

## [1.2.3] - 2026-09-22

### 추가됨

- **탐색기에 현재 열린 문서 표시**: 미리보기에 떠 있는 문서의 트리 행에 왼쪽 강조 막대와 파란 굵은 이름이 붙음. 문서를 연 뒤 트리에서 다른 폴더를 펼치거나 방향키로 돌아다녀도 어느 파일을 보고 있는지 계속 보임.
  - 기존의 선택 표시와는 별개로 동작함. 선택은 마지막으로 클릭하거나 방향키로 옮긴 행을 따라다니고 폴더 행에도 붙지만, 이 표시는 미리보기에 떠 있는 문서에만 붙음. 두 표시가 같은 행에 겹치면 선택 배경이 우선함.
  - 접혀 있던 폴더를 나중에 펼쳐도, 그 안의 열린 문서에 표시가 붙은 채로 나타남. 마크다운뿐 아니라 `.puml`·`.json`·텍스트 문서에도 동일하게 적용됨.
- **프리뷰 설치본 빌드 (`npm run dist:preview`)**: 다음 릴리스로 가는 도중의 변경을 실제로 설치해 쓰면서 확인하되, 그것이 릴리스인 척하지는 않게 하는 빌드. `1.3.0-preview.1`, `.2`, `.3`... 처럼 번호가 자동으로 올라감.
  - 버전은 `package.json`을 고치지 않고 빌드할 때만 주입함 - 리포지토리는 계속 마지막 릴리스 버전을 가리키고, 빌드된 앱만 프리뷰 버전을 보고함.
  - 프리리스 버전으로 빌드된 앱은 제목 표시줄에 버전을 함께 보여 줌(`MD Viewer — 1.3.0-preview.1`). 설치하면 기존 MD Viewer를 덮어쓰기 때문에 겉보기로는 구분되지 않아서, 정보 대화상자를 열지 않고도 지금 어느 빌드인지 알 수 있게 한 것. 릴리스 빌드의 제목은 그대로임.
  - 결과물은 `dist/preview/`에 따로 쌓여서 릴리스 인스톨러와 섞이지 않음. 프리뷰를 쓰다 되돌리려면 `dist/`의 릴리스 인스톨러를 다시 설치하면 됨.

## [1.2.2] - 2026-09-18

### 추가됨

- **미리보기 우클릭 메뉴 (복사 / 그림으로 복사)**: 뷰어에서 텍스트를 선택하고 우클릭하면 "복사", 그림 위에서 우클릭하면 "그림으로 복사"가 나옴. 렌더된 다이어그램(PlantUML·mermaid)뿐 아니라 문서가 가져온 이미지(`![](photo.png)`)와 `.svg` 파일에서도 동작함. 둘 다 해당되면 두 항목이 함께 나오고, 선택한 것도 없고 그림도 아니면 메뉴를 띄우지 않음.
  - 다이어그램과 SVG는 원래 크기의 2배 PNG로 만들어 흰 배경과 함께 클립보드에 넣음 - 문서나 메신저에 바로 붙여넣을 수 있음. SVG를 그대로 클립보드에 넣으면 대부분의 프로그램이 받지 못해서임. 아주 큰 그림은 픽셀 상한(1600만) 안에서 배율을 낮춤.
  - 가져온 사진·스크린샷은 다시 인코딩하지 않고 원본 픽셀 그대로 복사되므로 투명도도 유지됨.
  - 복사하면 툴바에 "그림으로 복사했습니다"가 표시됨.
- **편집기 안에서 찾기 (편집기에 커서가 있을 때 `Ctrl+F`)**: 소스 편집기에 커서를 둔 채 `Ctrl+F`를 누르면 렌더된 문서가 아니라 마크다운 원본에서 찾음. `**강조**`의 별표나 링크 URL처럼 렌더 결과에는 없는 텍스트도 찾을 수 있음.
  - 찾기 막대는 같은 것을 쓰고 대상만 바뀜(입력란 안내가 "소스에서 찾기"로 바뀜). `Enter`/`Shift+Enter`와 ▲▼로 이동하고, 모든 일치가 편집기에 노란색으로, 현재 항목은 더 진하게 표시됨.
  - 열 때 편집기에서 선택해 둔 한 줄짜리 텍스트가 있으면 그것으로 검색어를 채우고, 커서 위치 다음 일치부터 보여 줌. `Esc`로 닫으면 마지막으로 보던 일치에 커서를 놓고 편집기로 포커스를 돌려줌.
  - 뷰어에 포커스가 있을 때의 `Ctrl+F`는 기존과 동일하게 렌더된 문서에서 찾음. 사이드바 프로젝트 검색에서 결과를 클릭해 여는 경우도 기존대로 뷰어에서 표시됨.
- **뷰어에서 선택한 부분을 편집기에 표시**: 편집 모드(좌우 분할)에서 미리보기의 텍스트를 끌어 선택하면, 소스 편집기의 같은 부분이 청록색으로 표시됨. 렌더된 문장이 원본 어디에서 왔는지 바로 보임.
  - 문단 일부만 선택하면 그 문장만, 여러 블록에 걸쳐 선택하면 그 원본 구간 전체가 표시됨. `**강조**`처럼 원본과 렌더 결과가 다른 경우에도 원본에서 같은 글자를 찾아 표시하고, 표 셀이나 이미지 대체 텍스트처럼 원본에서 그대로 찾을 수 없으면 해당 블록 범위를 표시함.
  - 편집기의 실제 선택과 캐럿은 건드리지 않음. 텍스트 뒤 레이어에 칠하는 방식이라 편집 중 커서가 튀지 않고, 색도 일반 선택과 구분됨(텍스트가 선택된 게 아니라 위치를 가리키는 표시이므로). 편집기에 입력하거나 선택을 해제하면 표시도 사라짐.
- **앞으로 가기 (마우스 XButton2)**: 뒤로 가기로 돌아온 뒤, 마우스의 앞으로 버튼으로 되돌린 만큼 다시 진행할 수 있음. 뒤로 가기와 정확히 대칭으로 동작해서, 문서 이동뿐 아니라 문서 안 앵커 이동(목차 클릭·§ 링크·본문 `#` 링크)도 되돌린 순서 그대로 다시 적용됨.
  - 브라우저와 같은 규칙: 뒤로 간 뒤에 새로 이동하면(링크 클릭, 트리에서 문서 선택, 새 앵커 점프) 앞으로 갈 기록은 사라짐.
  - 뒤로 가기와 마찬가지로 OS가 보내는 앞으로 명령과 DOM 마우스 버튼 두 경로 모두에서 동작함.

### 변경됨

- **`npm start`에 열 폴더/파일 인자 전달**: `main.js`는 argv의 경로 인자를 처리하고 있었지만 `scripts/start.js`가 electron을 항상 `.` 하나만으로 띄워 인자가 전달되지 않았음. 이제 `npm start -- <경로>`로 특정 폴더·문서를 바로 열 수 있음.
- **PlantUML 렌더링이 크게 빨라짐 (렌더 서버 재사용)**: 다이어그램마다 JVM을 새로 띄우던 방식을, 번들된 `plantuml.jar`의 내장 HTTP 렌더 서버 하나를 띄워 재사용하는 방식으로 바꿈. 같은 jar를 쓰므로 나오는 SVG는 바이트 단위로 동일함.
  - 이 기계에서 측정한 값: 작은 다이어그램 2,568ms → 15ms, 큰 다이어그램(SVG 188KB) 3,565ms → 150ms, 다이어그램 6개 동시 6,041ms → 18ms.
  - 시간의 대부분이 그리기가 아니라 자바 기동이었음: JVM 기동 1.66초 + 29MB jar 열기 0.65초 ≈ 2.3초인데 실제 그리기는 0.3초. JVM 옵션 튜닝은 효과가 없었음.
  - 서버는 첫 다이어그램을 그릴 때 시작하고(약 1초), 5분간 쓰이지 않으면 종료해 메모리(약 144MB)를 돌려줌. 창을 닫으면 함께 정리됨.
  - `127.0.0.1`에만 바인딩함 - 기본값은 모든 인터페이스라서, 로컬 파일을 읽을 수 있는(`!include`) 렌더러가 네트워크에 열려 버림.
  - 서버가 시작되지 않거나 죽으면 기존의 다이어그램별 JVM 방식으로 자동 폴백하므로 렌더가 실패하지는 않음. 계속 실패하면 재시도 간격을 늘려, 매 문서마다 기다리지 않게 함.
- **같은 다이어그램은 다시 그리지 않음**: 원본이 같으면 이전 결과를 재사용함. 편집 모드에서는 키를 누를 때마다 문서 전체가 다시 렌더되면서 다이어그램도 매번 다시 그려졌는데, 이제 다이어그램을 건드리지 않은 편집은 다이어그램 렌더 비용이 사라짐(다이어그램 6개 문서에서 17ms, 대부분 마크다운 파싱 시간).

## [1.2.1] - 2026-09-16

### 수정됨

- **`<br/>`가 들어간 mermaid 다이어그램이 깨지던 문제**: mermaid는 라벨을 `<foreignObject>` 안의 HTML로 그리기 때문에 `<br/>`를 쓴 라벨이 닫히지 않은 `<br>`로 나옴. 이는 HTML로는 유효하지만 XML로는 아니고, 미리보기는 다이어그램을 `data:image/svg+xml` 이미지로 넣기 때문에 XML 파서가 문서 전체를 거부해 이미지가 아예 표시되지 않았음(같은 이유로 크기 보정도 건너뛰어졌음). 이제 SVG를 HTML로 파싱한 뒤 XML로 다시 직렬화해 태그가 모두 닫힌 상태로 넣음 - 라벨은 그대로 두고 깨짐만 사라짐.

## [1.2.0] - 2026-09-16

### 추가됨

- **프로젝트 내 문서 검색 (Ctrl+Shift+F)**: 사이드바에서 열려 있는 프로젝트의 모든 문서(`.md`/`.markdown`/`.puml`/`.json`/`.txt`/`.log` + 사용자 지정 확장자) 본문을 한 번에 검색. 대/소문자 구분·단어 단위·정규식 옵션 제공.
  - 결과는 파일별로 묶여 일치한 줄과 줄 번호를 함께 표시하고, 그룹 헤더를 눌러 접을 수 있음. 검색 결과를 클릭(또는 ↑/↓ + Enter)하면 해당 문서를 열고 그 위치로 이동하면서 "문서 내 찾기" 하이라이트까지 이어짐.
  - 숨김 폴더와 `node_modules`, 2MB 초과 파일, 바이너리는 건너뜀. 결과가 과도하게 많으면 잘라내고 그 사실을 상태 줄에 표시.
- **파일 트리 우클릭에 "절대경로 복사" / "상대경로 복사" 추가**: "탐색기에서 상위 폴더 열기" 아래 별도 그룹으로 표시. 절대경로는 OS 형식 그대로(`D:\...\guide.md`), 상대경로는 프로젝트 루트 기준에 슬래시(`docs/guide.md`)로 복사되어 마크다운 링크에 바로 붙여넣을 수 있음. 빈 공간을 우클릭하면 프로젝트 루트가 대상이 되며 이때 상대경로는 루트 폴더 이름이 됨.
- **뷰어 확대/축소 (Ctrl+휠)**: 미리보기 영역에서 `Ctrl`+휠로 문서 전체를 확대/축소. 50%~300% 사이 13단계로, 커서 아래 지점이 그대로 유지되도록 스크롤을 맞춰 줌. 본문·이미지·표·JSON 트리·다이어그램이 함께 커지고 사이드바와 툴바는 원래 크기를 유지함.
  - `Ctrl+0` 또는 툴바에 나타나는 배율 표시(예: `150%`)를 클릭하면 100%로 복귀. 배율은 프로젝트별로 저장되어 다시 열 때 그대로 복원됨.
  - PlantUML 다이어그램 위에서는 기존처럼 다이어그램만 확대/축소되고 문서 배율은 그대로.
- **PlantUML 파일을 SVG로 변환**: 파일 트리에서 `.puml` 파일 우클릭 > "SVG로 변환". 저장 위치를 묻지 않고 **원본과 같은 폴더에 같은 이름**(`guide.puml` → `guide.svg`)으로 바로 저장하며, 이미 있으면 덮어씀. 미리보기와 같은 번들 `plantuml.jar`로 렌더링하므로 외부 서버 없이 오프라인에서도 동작하고 큰 다이어그램도 잘리지 않음.
  - 대화상자가 뜨지 않는 대신, 트리에서 해당 폴더가 갱신되어 만들어진 `.svg`가 바로 보이고 툴바에 "SVG로 변환했습니다: guide.svg" 메시지가 표시됨.
- **현재 문서 전체화면으로 보기 (F11)**: 보기 메뉴 > "현재 문서 전체화면으로 보기", `F11`, 또는 툴바의 ⛶ 버튼으로 전환. 창이 실제 OS 전체화면으로 바뀌고, 문서 주변의 탐색기·툴바·메뉴 바·CSS 편집기·터미널·소스 편집기가 전부 숨겨져 본문만 남음. 읽는 데 도움이 되는 목차 패널은 그대로 유지됨.
  - `Esc` 또는 `F11`, 왼쪽 위의 ✕ 버튼으로 종료. 들어갈 때 종료 방법을 알려 주는 안내가 잠깐 떴다 사라짐.
  - `Ctrl+F` 찾기와 `Ctrl`+휠 확대/축소는 전체화면에서도 그대로 동작하며, 찾기가 열려 있으면 `Esc`가 찾기부터 닫음. 숨겨진 패널을 여는 명령(편집 모드·CSS 편집기·터미널·프로젝트 검색)을 쓰면 전체화면이 자동으로 해제됨.
  - 보는 방식일 뿐 레이아웃이 아니므로 프로젝트 상태로 저장하지 않음. 열려 있던 패널 구성은 전체화면을 빠져나오면 그대로 복원됨.
- **목차 항목별 접기**: 목차 패널에서 하위 제목을 가진 항목 왼쪽에 접기 화살표(▶)가 표시되어, 눌러서 그 아래 하위 제목들을 접거나 펼칠 수 있음. 긴 문서에서 필요한 절만 펼쳐 두고 볼 수 있음.
  - 화살표만 접기/펼치기를 하고, 항목의 나머지 부분을 누르면 기존처럼 해당 제목 위치로 이동함.
  - 제목 단계를 건너뛴 문서(`#` 다음에 바로 `###`)도 실제 포함 관계대로 접힘. 상위 항목을 접으면 그 아래는 개별적으로 펼쳐 둔 항목까지 함께 숨겨지고, 다시 펼치면 안쪽에서 접어 둔 상태가 그대로 살아남.
  - 접은 상태는 문서별로 기억되어, 파일이 외부에서 바뀌어 다시 그려지거나 편집 모드를 오가거나 다른 문서에 다녀와도 유지됨. 문서에서 사라진 제목의 접힘 상태는 정리됨.
- **파일 끌어다 놓기 (드래그 앤 드롭)**: 놓는 위치에 따라 동작이 다름.
  - **탐색기(트리) 영역에 놓으면 프로젝트로 복사**: 폴더 행 위에 놓으면 그 폴더로, 파일 행 위에 놓으면 그 파일이 있는 폴더로, 빈 공간에 놓으면 프로젝트 루트로 복사됨. 놓기 전에 대상 폴더가 강조 표시되고 하단에 폴더 이름이 표시됨. 폴더를 통째로 놓으면 하위 내용까지 복사되며, 여러 개를 한 번에 놓을 수 있음.
  - **뷰어 영역에 놓으면 바로 보기**: 프로젝트에 복사하지 않고 그 문서를 즉시 렌더링해서 보여 줌. 프로젝트 밖 파일이면 툴바에 "임시 보기" 표시가 뜨고, 해당 파일이 프로젝트의 "마지막 연 문서"로 저장되지 않음. 프로젝트 안의 파일을 놓으면 평소처럼 열리고 트리에서 선택됨.
  - 같은 이름이 이미 있으면 **덮어쓰지 않고** `이름 (2).md`처럼 번호를 붙여 복사하고, 이름이 바뀐 개수를 알려 줌. 폴더를 자기 자신이나 그 하위 폴더로 놓는 경우는 건너뜀.
  - 볼 수 없는 형식(이미지 등)을 뷰어에 놓으면 그 사실을 알려 주고, 폴더를 뷰어에 놓으면 프로젝트로 엶.


### 변경됨

- **`.puml` 문서는 가로폭 제한 없이 표시**: `.puml` 파일은 문서 전체가 다이어그램 하나이므로, 본문용 읽기 폭(900px)에 가두지 않고 JSON·일반 텍스트처럼 미리보기 영역 전체를 쓰도록 함. 넓은 다이어그램이 좁은 칸 안에서 가로 스크롤되는 대신 창 폭만큼 펼쳐짐. (마크다운 문서 안의 ```puml 펜스는 기존 그대로 본문 폭을 따름.)
- **Mermaid 다이어그램을 앱 안에서 렌더링**: 이전에는 ```mermaid 코드 펜스를 공개 `mermaid.ink` 서버로 인코딩해 보내고 이미지를 받아 왔음. 이제 번들된 mermaid를 숨은 렌더 창에서 돌려 로컬에서 SVG를 만들어 씀.
  - 문서 내용이 외부로 나가지 않고, 오프라인에서도 동작하며, 서버 상태나 URL 길이 제한에 영향을 받지 않음. PDF 내보내기도 같은 경로를 쓰므로 미리보기와 같은 그림이 나옴.
  - 구문이 잘못된 다이어그램은 PlantUML처럼 빨간 오류 상자에 파서 메시지를 보여 줌.
  - 미리보기 폭보다 넓은 다이어그램은 가로 스크롤을 만들지 않고 폭에 맞춰 축소해서 보여 줌(작은 다이어그램은 원래 크기 유지). 자세히 볼 때는 `Ctrl`+휠로 확대.
  - mermaid 런타임은 PlantUML과 같은 방식으로 준비됨: `npm start` / `npm run dist`가 `thirdparty/mermaid/`에 자동으로 복사하고(`npm run prepare:mermaid`), 설치 파일에 함께 들어감. mermaid 자체는 devDependency라 패키지 전체가 설치 파일에 실리지 않음.

### 수정됨

- **터미널 우클릭 붙여넣기가 한 번만 동작**: Claude Code·vim·htop처럼 마우스 추적(mouse tracking)을 켜는 프로그램을 터미널에서 실행하면, 우클릭의 누름과 뗌이 각각 이스케이프 시퀀스로 셸에 전달되어 그 프로그램이 붙여넣기를 한 번씩 더 수행했음(즉 한 번 우클릭에 붙여넣기가 두 번). 이 앱에서 터미널 우클릭은 복사/붙여넣기 제스처이므로, 오른쪽 버튼 이벤트를 xterm에 닿기 전 캡처 단계에서 막아 셸로 보고되지 않게 함. 이제 어떤 프로그램이 실행 중이든 우클릭은 한 번의 복사/붙여넣기로 동작함.
- **테마를 바꿔도 작업 상태가 유지됨**: 이전에는 설정 > 테마를 바꿀 때마다 창을 새로고침해서 열려 있던 프로젝트·문서·편집 중이던 내용이 모두 사라지고 시작 화면으로 돌아갔음. 테마 색상은 전부 `prefers-color-scheme` 미디어 쿼리라 새로고침 없이도 셸과 미리보기 iframe이 그 자리에서 다시 그려지므로, 새로고침을 제거함. 저장하지 않은 편집 내용, 터미널 세션, 스크롤 위치까지 그대로 남음.
- **언어 변경 후 열려 있던 프로젝트로 복귀**: 언어 변경은 UI 문자열을 다시 적용해야 해서 새로고침이 여전히 필요하지만, 새로고침 후 열려 있던 프로젝트를 자동으로 다시 열어 마지막 문서·스크롤 위치·편집 모드까지 복원함 (저장하지 않은 편집 내용은 새로고침으로 사라짐).

## [1.1.0] - 2026-08-04

### 추가됨

- **PlantUML 로컬 렌더링**: 공개 plantuml.com 서버 대신 번들된 `plantuml.jar` + 최소 jlink JRE로 로컬 렌더링. 큰 다이어그램에서 서버의 URL 길이 제한(~8KB)에 걸려 깨지던 문제 해결.
  - 렌더링을 비동기 프로세스로 전환해 큰 다이어그램을 그리는 동안 앱 전체가 멈추는 문제도 함께 해결. 로딩 표시(스피너) 추가.
  - 페이지를 이동하거나 같은 파일을 다시 렌더링하면, 이전 렌더 요청은 결과를 버리는 것은 물론 실행 중인 프로세스 자체를 종료(cancel)하도록 처리.
- **PlantUML 다이어그램 확대/축소·이동**: 다이어그램 우상단 확대(+)/축소(-)/리셋 버튼, `Ctrl+휠` 확대/축소, 드래그 이동(pan).
- **문서 내 찾기 (Ctrl+F)**: 뷰어(미리보기) 안의 텍스트만 검색·하이라이트하고 다음/이전 이동. 편집 모드에서는 찾은 위치에 맞춰 소스 에디터도 함께 스크롤·선택되어 동기화됨.
- **에디터 ↔ 뷰어 스크롤 상시 동기화**: 편집 모드(좌우 분할)에서 한쪽을 스크롤하면 다른 쪽도 같은 위치로 따라감.
- **새로고침 버튼 일반화**: 기존에는 PlantUML 파일에만 동작하던 새로고침을 모든 파일 종류(마크다운/JSON/텍스트)에서 디스크로부터 다시 불러오도록 확장. 외부 편집기에서 변경한 내용을 편집 모드 중에도 반영 가능.
- **PDF 내보내기**: 파일 트리에서 `.md` 파일 우클릭 → "PDF로 내보내기...". 미리보기와 동일하게 렌더링한 뒤 항상 라이트 테마로 PDF 생성.
- **코드 블록 색상을 VS Code와 정확히 일치**: 라이트/다크 모드 모두 VS Code의 실제 "Default Light/Dark 2026" 테마 색상값 그대로 적용 (하이라이트 안 되던 항목·서로 색이 겹치던 항목 수정).
- **테마 메뉴 (설정 > 테마)**: 시스템 설정 따르기 / 라이트 / 다크 중 선택 가능, 앱 재시작 후에도 유지.
- **마크다운 표 컬럼 폭 반응형 처리**: 이전에는 모든 컬럼이 균등 분할되어 비효율적이었음. 첫 컬럼은 25% 기본값(5열 이상이면 18%, 8열 이상이면 힌트 제거)으로 시작하고, 나머지 컬럼은 내용에 따라 자동으로 폭 조정.
- **파일 트리에서 새 파일/새 폴더 생성**: 빈 공간·폴더·파일 우클릭 시 "새 파일...", "새 폴더..." 메뉴 제공. 인라인 입력으로 이름을 지정하면 즉시 생성되고, 파일이면 바로 열림.

### 변경됨

- 새로고침(🔄) 버튼이 더 이상 PlantUML 파일에서만 보이지 않고, 파일이 열려 있으면 항상 표시됨.

## [1.0.0] - 2026-07-28

- 초기 릴리스: 프로젝트 트리뷰, 마크다운/JSON/PUML/일반 텍스트 렌더링 및 편집, 커스텀 CSS, 내장 터미널, 파일 자동 감시, Windows 설치 파일(NSIS) 및 탐색기 연동.
