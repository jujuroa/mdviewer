# 변경 이력

이 프로젝트의 주요 변경 사항을 버전별로 기록합니다.
형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.0.0/)를 참고합니다.

## [Unreleased]

### 추가됨

- **프로젝트 내 문서 검색 (Ctrl+Shift+F)**: 사이드바에서 열려 있는 프로젝트의 모든 문서(`.md`/`.markdown`/`.puml`/`.json`/`.txt`/`.log` + 사용자 지정 확장자) 본문을 한 번에 검색. 대/소문자 구분·단어 단위·정규식 옵션 제공.
  - 결과는 파일별로 묶여 일치한 줄과 줄 번호를 함께 표시하고, 그룹 헤더를 눌러 접을 수 있음. 검색 결과를 클릭(또는 ↑/↓ + Enter)하면 해당 문서를 열고 그 위치로 이동하면서 "문서 내 찾기" 하이라이트까지 이어짐.
  - 숨김 폴더와 `node_modules`, 2MB 초과 파일, 바이너리는 건너뜀. 결과가 과도하게 많으면 잘라내고 그 사실을 상태 줄에 표시.
- **파일 트리 우클릭에 "절대경로 복사" / "상대경로 복사" 추가**: "탐색기에서 상위 폴더 열기" 아래 별도 그룹으로 표시. 절대경로는 OS 형식 그대로(`D:\...\guide.md`), 상대경로는 프로젝트 루트 기준에 슬래시(`docs/guide.md`)로 복사되어 마크다운 링크에 바로 붙여넣을 수 있음. 빈 공간을 우클릭하면 프로젝트 루트가 대상이 되며 이때 상대경로는 루트 폴더 이름이 됨.
- **뷰어 확대/축소 (Ctrl+휠)**: 미리보기 영역에서 `Ctrl`+휠로 문서 전체를 확대/축소. 50%~300% 사이 13단계로, 커서 아래 지점이 그대로 유지되도록 스크롤을 맞춰 줌. 본문·이미지·표·JSON 트리·다이어그램이 함께 커지고 사이드바와 툴바는 원래 크기를 유지함.
  - `Ctrl+0` 또는 툴바에 나타나는 배율 표시(예: `150%`)를 클릭하면 100%로 복귀. 배율은 프로젝트별로 저장되어 다시 열 때 그대로 복원됨.
  - PlantUML 다이어그램 위에서는 기존처럼 다이어그램만 확대/축소되고 문서 배율은 그대로.
- **현재 문서 전체화면으로 보기 (F11)**: 보기 메뉴 > "현재 문서 전체화면으로 보기", `F11`, 또는 툴바의 ⛶ 버튼으로 전환. 창이 실제 OS 전체화면으로 바뀌고, 문서 주변의 탐색기·툴바·메뉴 바·CSS 편집기·터미널·소스 편집기가 전부 숨겨져 본문만 남음. 읽는 데 도움이 되는 목차 패널은 그대로 유지됨.
  - `Esc` 또는 `F11`, 왼쪽 위의 ✕ 버튼으로 종료. 들어갈 때 종료 방법을 알려 주는 안내가 잠깐 떴다 사라짐.
  - `Ctrl+F` 찾기와 `Ctrl`+휠 확대/축소는 전체화면에서도 그대로 동작하며, 찾기가 열려 있으면 `Esc`가 찾기부터 닫음. 숨겨진 패널을 여는 명령(편집 모드·CSS 편집기·터미널·프로젝트 검색)을 쓰면 전체화면이 자동으로 해제됨.
  - 보는 방식일 뿐 레이아웃이 아니므로 프로젝트 상태로 저장하지 않음. 열려 있던 패널 구성은 전체화면을 빠져나오면 그대로 복원됨.
- **목차 항목별 접기**: 목차 패널에서 하위 제목을 가진 항목 왼쪽에 접기 화살표(▶)가 표시되어, 눌러서 그 아래 하위 제목들을 접거나 펼칠 수 있음. 긴 문서에서 필요한 절만 펼쳐 두고 볼 수 있음.
  - 화살표만 접기/펼치기를 하고, 항목의 나머지 부분을 누르면 기존처럼 해당 제목 위치로 이동함.
  - 제목 단계를 건너뛴 문서(`#` 다음에 바로 `###`)도 실제 포함 관계대로 접힘. 상위 항목을 접으면 그 아래는 개별적으로 펼쳐 둔 항목까지 함께 숨겨지고, 다시 펼치면 안쪽에서 접어 둔 상태가 그대로 살아남.
  - 접은 상태는 문서별로 기억되어, 파일이 외부에서 바뀌어 다시 그려지거나 편집 모드를 오가거나 다른 문서에 다녀와도 유지됨. 문서에서 사라진 제목의 접힘 상태는 정리됨.
- **파일 끌어다 놓기 (드래그 앤 드롭)**: 놓는 위치에 따라 동작이 다름.
  - **탐색기(트리) 영역에 놓으면 프로젝트로 복사**: 폴더 행 위에 놓으면 그 폴더로, 파일 행 위에 놓으면 그 파일이 있는 폴더로, 빈 공간에 놓으면 프로젝트 루트로 복사됨. 놓기 전에 대상 폴더가 강조 표시되고 하단에 폴더 이름이 표시됨. 폴더를 통째로 놓으면 하위 내용까지 복사되며, 여러 개를 한 번에 놓을 수 있음.
  - **뷰어 영역에 놓으면 바로 보기**: 프로젝트에 복사하지 않고 그 문서를 즉시 렌더링해서 보여 줌. 프로젝트 밖 파일이면 툴바에 "임시 보기" 표시가 뜨고, 해당 파일이 프로젝트의 "마지막 연 문서"로 저장되지 않음. 프로젝트 안의 파일을 놓으면 평소처럼 열리고 트리에서 선택됨.
  - 같은 이름이 이미 있으면 **덮어쓰지 않고** `이름 (2).md`처럼 번호를 붙여 복사하고, 이름이 바뀐 개수를 알려 줌. 폴더를 자기 자신이나 그 하위 폴더로 놓는 경우는 건너뜀.
  - 볼 수 없는 형식(이미지 등)을 뷰어에 놓으면 그 사실을 알려 주고, 폴더를 뷰어에 놓으면 프로젝트로 엶.


### 수정됨

- **테마를 바꿔도 작업 상태가 유지됨**: 이전에는 설정 > 테마를 바꿀 때마다 창을 새로고침해서 열려 있던 프로젝트·문서·편집 중이던 내용이 모두 사라지고 시작 화면으로 돌아갔음. 테마 색상은 전부 `prefers-color-scheme` 미디어 쿼리라 새로고침 없이도 셸과 미리보기 iframe이 그 자리에서 다시 그려지므로, 새로고침을 제거함. 저장하지 않은 편집 내용, 터미널 세션, 스크롤 위치까지 그대로 남음.
- **언어 변경 후 열려 있던 프로젝트로 복귀**: 언어 변경은 UI 문자열을 다시 적용해야 해서 새로고침이 여전히 필요하지만, 새로고침 후 열려 있던 프로젝트를 자동으로 다시 열어 마지막 문서·스크롤 위치·편집 모드까지 복원함 (저장하지 않은 편집 내용은 새로고침으로 사라짐).

## [1.1.0] - 2026-08-04

### 추가됨

- **PlantUML 로컬 렌더링**: 공개 plantuml.com 서버 대신 번들된 `plantuml.jar` + 최소 jlink JRE로 로컬 렌더링. 큰 다이어그램에서 서버의 URL 길이 제한(~8KB)에 걸려 깨지던 문제 해결.
  - 렌더링을 비동기 프로세스로 전환해 큰 다이어그램을 그리는 동안 앱 전체가 멈추는 문제도 함께 해결. 로딩 표시(스피너) 추가.
  - 페이지를 이동하거나 같은 파일을 다시 렌더링하면, 이전 렌더 요청은 결과를 버리는 것은 물론 실행 중인 프로세스 자체를 종료(cancel)하도록 처리.
- **PlantUML 다이어그램 확대/축소·이동**: 다이어그램 우상단 확대(+)/축소(-)/리셋 버튼, `Ctrl+휠` 확대/축소, 드래그 이동(pan).
- **문서 내 찾기 (Ctrl+F)**: 뷰어(미리보기) 안의 텍스트만 검색·하이라이트하고 다음/이전 이동. 편집 모드에서는 찾은 위치에 맞춰 소스 에디터도 함께 스크롤·선택되어 동기화됨.
- **에디터 ↔ 뷰어 스크롤 상시 동기화**: 편집 모드(좌우 분할)에서 한쪽을 스크롤하면 다른 쪽도 같은 위치로 따라감.
- **새로고침 버튼 일반화**: 기존에는 PlantUML 파일에만 동작하던 새로고침을 모든 파일 종류(마크다운/JSON/텍스트)에서 디스크로부터 다시 불러오도록 확장. 외부 편집기에서 변경한 내용을 편집 모드 중에도 반영 가능.
- **PDF 내보내기**: 파일 트리에서 `.md` 파일 우클릭 → "PDF로 내보내기...". 미리보기와 동일하게 렌더링한 뒤 항상 라이트 테마로 PDF 생성.
- **코드 블록 색상을 VS Code와 정확히 일치**: 라이트/다크 모드 모두 VS Code의 실제 "Default Light/Dark 2026" 테마 색상값 그대로 적용 (하이라이트 안 되던 항목·서로 색이 겹치던 항목 수정).
- **테마 메뉴 (설정 > 테마)**: 시스템 설정 따르기 / 라이트 / 다크 중 선택 가능, 앱 재시작 후에도 유지.
- **마크다운 표 컬럼 폭 반응형 처리**: 이전에는 모든 컬럼이 균등 분할되어 비효율적이었음. 첫 컬럼은 25% 기본값(5열 이상이면 18%, 8열 이상이면 힌트 제거)으로 시작하고, 나머지 컬럼은 내용에 따라 자동으로 폭 조정.
- **파일 트리에서 새 파일/새 폴더 생성**: 빈 공간·폴더·파일 우클릭 시 "새 파일...", "새 폴더..." 메뉴 제공. 인라인 입력으로 이름을 지정하면 즉시 생성되고, 파일이면 바로 열림.

### 변경됨

- 새로고침(🔄) 버튼이 더 이상 PlantUML 파일에서만 보이지 않고, 파일이 열려 있으면 항상 표시됨.

## [1.0.0] - 2026-07-28

- 초기 릴리스: 프로젝트 트리뷰, 마크다운/JSON/PUML/일반 텍스트 렌더링 및 편집, 커스텀 CSS, 내장 터미널, 파일 자동 감시, Windows 설치 파일(NSIS) 및 탐색기 연동.
